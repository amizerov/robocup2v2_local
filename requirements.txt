# -----------------------------------------------------------------------
# ВАЖНО: этот файл должен использоваться с Python 3.14.x
# Боевой сервер работает на Python 3.14. cloudpickle сохраняет байткод
# методов, который несовместим между минорными версиями Python.
# Модель, собранная не на 3.14, не пройдёт валидацию на сайте.
# -----------------------------------------------------------------------

# --- simulation core ---
gymnasium>=1.0,<2.0
pygame-ce>=2.5,<3.0

# --- web server ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
python-multipart>=0.0.9

# --- serialization (должна совпадать с версией на сервере) ---
cloudpickle==3.1.2

# --- numerics ---
numpy>=2.0,<3.0

# --- optional: install if your model uses these ---
# torch>=2.2
# stable-baselines3>=2.4
# scikit-learn>=1.4
# scipy>=1.13
# pandas>=2.2
# optuna>=3.6
